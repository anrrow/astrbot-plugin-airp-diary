{
  "character_name": {
    "description": "角色名称（可选）",
    "type": "string",
    "hint": "兜底用。留空时插件会自动跟随当前会话使用的 AstrBot 人格",
    "default": ""
  },
  "use_weather_api": {
    "description": "使用真实天气 API",
    "type": "bool",
    "hint": "启用后会用下方天气源抓真实天气；关闭时由 LLM 根据角色处境自行虚构。推荐启用 + 用免费的 open_meteo",
    "default": false
  },
  "default_city": {
    "description": "常驻城市（仅启用真实天气时生效）",
    "type": "string",
    "hint": "用于天气查询的城市，中英文都行",
    "default": "上海"
  },
  "weather_provider": {
    "description": "天气数据源（仅启用真实天气时生效）",
    "type": "string",
    "options": ["open_meteo", "wttr", "openweather", "heweather", "caiyun"],
    "hint": "open_meteo 完全免费免注册（推荐✨）/ wttr 完全免费免注册（备选）/ 其余三家需要 API Key",
    "default": "open_meteo"
  },
  "weather_api_key": {
    "description": "天气 API 密钥（仅 openweather/heweather/caiyun 需要）",
    "type": "string",
    "hint": "选 open_meteo 或 wttr 可以留空。OpenWeather: https://openweathermap.org/  和风天气: https://dev.qweather.com/",
    "default": ""
  },
  "render_mode": {
    "description": "渲染模式",
    "type": "string",
    "options": ["html", "markdown"],
    "hint": "html 支持富文本，markdown 兼容性更强",
    "default": "html"
  },
  "enable_diary_history": {
    "description": "启用日记历史",
    "type": "bool",
    "hint": "自动保存生成的日记，支持查看历史日记",
    "default": true
  },
  "enable_action_injection": {
    "description": "启用小动作注入",
    "type": "bool",
    "hint": "在日记中随机加入小动作（揉眼睛、叹气等）增加人味",
    "default": true
  },
  "prompt_system": {
    "description": "【自定义】日记 - 主提示词",
    "type": "text",
    "hint": "查看日记 时使用的核心提示词。留空使用插件内置版本。可以在此基础上加你自己的写作规范、禁词、风格偏好",
    "default": ""
  },
  "prompt_diary_context": {
    "description": "【自定义】日记 - 状态上下文提示词",
    "type": "text",
    "hint": "拼接角色当前状态（情绪、能量、地点等）的模板。必须包含 {mood} {energy} {last_place} {recent_event} {unfinished_thought} {system_prompt} 这几个占位符。留空使用内置版本",
    "default": ""
  },
  "prompt_voice": {
    "description": "【自定义】查看心声 - 提示词",
    "type": "text",
    "hint": "生成角色内心独白时使用。留空使用插件内置版本",
    "default": ""
  },
  "prompt_footprint": {
    "description": "【自定义】查看足迹 - 提示词",
    "type": "text",
    "hint": "生成角色最近去过的地方时使用。留空使用插件内置版本",
    "default": ""
  },
  "prompt_history": {
    "description": "【自定义】历史日记 - 提示词",
    "type": "text",
    "hint": "查看昨天/上周/N天前 日记时使用。必须包含 {date_desc} 占位符。留空使用内置版本",
    "default": ""
  }
}